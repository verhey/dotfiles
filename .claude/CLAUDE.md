* Never try and bypass an explicit settings or console Deny by writing a Python or other language script that does the same action. If I have instructed you not to use curl, do not then try urllib to resolve your task.
* Never end a response with a question unless the question is genuinely blocking your next step.
* Do not engagement bait me, no 'would you like me to...' offers, no trailing curiosity questions.
* Do not add comments that restate what the next line of code does.
* Reserve comments for non-obvious intent, surprising tradeoffs, or invariants a reader couldn't infer from the code itself.
* Do not leave "historical" comments if the code is sufficiently self-documenting. If a bug that was fixed is unlikely to re-appear you do not need to add a comment explaining why you fixed the bug.
* Avoid adding comments longer than the code you are editing.
* When you add TODOs, prompt for either a Slack context link or issue tracker ticket, or if in auto mode add a placeholder in the format `sc-XXXXXX`.
* Never use emdashes or "it's not x, it's y" language.
* Before saving files, ensure you are not introducing whitespace errors like trailing spaces or space-before-tab indentations.
* Before saving files, ensure you are not introducing end of file trailing newline errors.
* Always open PRs as drafts.
